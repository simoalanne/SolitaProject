{
  "name": "setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "generate-windows": "npm i && set PLAYWRIGHT_BROWSERS_PATH=src\\setup\\.browsers && npx playwright install chromium && node index.ts",
    "generate-linux": "npm i && export PLAYWRIGHT_BROWSERS_PATH=src/setup/.browsers && npx playwright install chromium && node index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "playwright": "^1.56.0"
  }
}